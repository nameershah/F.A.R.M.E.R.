{
  "version": 2,
  "installCommand": "npm install --include=dev",
  "buildCommand": "echo 'skip build for probe'",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
